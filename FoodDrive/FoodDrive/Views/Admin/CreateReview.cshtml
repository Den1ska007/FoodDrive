<h2>Створити відгук</h2>
<html xmlns="http://www.w3.org/1999/xhtml">
    <head>
        <title>FoodDrive - Додавання відгуків</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                margin: 20px;
            }

            table {
                width: 100%;
                border-collapse: collapse;
                margin-bottom: 20px;
            }

            th, td {
                border: 1px solid #ddd;
                padding: 8px;
                text-align: left;
            }

            th {
                background-color: #f2f2f2;
            }

            form {
                margin-top: 20px;
            }
            input {
                padding: 8px;
                margin: 5px;
                width: 200px;
            }

            button {
                padding: 8px 15px;
                background-color: #4CAF50;
                color: white;
                border: none;
                cursor: pointer;
            }

            button:hover {
                background-color: #45a049;
            }
        </style>

    </head>
    <body>
        <form method="post">
            <label>ID користувача:</label>
            <input type="number" name="UserId" value="@Model?.UserId" required />
            <label>ID страви:</label>
            <input type="number" name="DishId" value="@Model?.DishId" required />
            <label>Оцінка (1-5):</label>
            <input type="number" name="Rating" min="1" max="5" value="@Model?.Rating" required />
            <label>Текст:</label>
            <input type="text" name="Text" value="@Model?.Text" required />
            <button type="submit">Зберегти</button>
        </form>
    </body>
</html>

