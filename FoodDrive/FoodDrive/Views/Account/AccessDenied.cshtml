@{
    ViewData["Title"] = "Доступ заборонено";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-danger text-white">
                    <h3 class="card-title">@ViewData["Title"]</h3>
                </div>
                <div class="card-body text-center">
                    <div class="mb-4">
                        <i class="fas fa-ban fa-5x text-danger"></i>
                    </div>
                    <h4>У вас немає доступу до цього ресурсу</h4>
                    <p>Для доступу до цієї сторінки потрібні відповідні права.</p>

                    <div class="d-grid gap-2">
                        @if (User.Identity.IsAuthenticated)
                        {
                            <a asp-controller="Admin" asp-action="Index" class="btn btn-primary">На головну</a>
                        }
                        else
                        {
                            <a asp-controller="Account" asp-action="Login" class="btn btn-primary">Увійти в систему</a>
                        }
                        <a href="mailto:support@fooddrive.com" class="btn btn-outline-secondary">Зв'язатися з підтримкою</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>